plugins {
    // Lets Gradle provision a matching JDK automatically when none is installed locally.
    id("org.gradle.toolchains.foojay-resolver-convention") version "1.0.0"
}

rootProject.name = "lo-speziale"
